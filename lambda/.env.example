# Local-only — the deployed Lambda reads these from AWS Secrets Manager
# (SUBMISSIONS_SECRET_ARN) and the CFN-provided CV_BUCKET_NAME instead.

CV_BUCKET_NAME=
SUBMISSIONS_SECRET_ARN=

# Same two keys as the Secrets Manager JSON blob, for local testing without AWS.
NOTION_API_KEY=
NOTION_SUBMISSIONS_DATA_SOURCE_ID=
