// Matches "/" so that navigating home clears the sheet slot (a soft nav to a
// route the slot no longer matches would otherwise keep the sheet visible).
export default function Page() {
  return null;
}
